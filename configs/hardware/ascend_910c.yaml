name: "Ascend 910C"
peak_tflops_bf16: 800
hbm_capacity_gb: 128
hbm_bandwidth_tb_s: 3.2
hbm_usable_ratio: 0.85
cpu_gpu_bw_gb_s: 32
calibration:
  compute_eff_large_gemm: 0.50
  compute_eff_small_op: 0.20
  memory_efficiency: 0.70
  comm_efficiency: 0.70
  # CP communication-group init cost: T_init(g) = alpha + beta*g + gamma*g*log2(g)
  # per group, plus cp_group_buffer_mb resident HBM per communicator. Intra-node
  # (HCCS) small-group illustrative values; one-time at init, not per-step.
  cp_init_alpha_s: 0.05
  cp_init_beta_s: 0.005
  cp_init_gamma_s: 0.002
  cp_group_buffer_mb: 30
